[project]
name = "euler-provider-catalog"
version = "0.2.0"
description = "Deterministic provider and model metadata for Euler"
requires-python = ">=3.11"
license = "MIT"

[tool.ruff]
line-length = 100
target-version = "py311"
